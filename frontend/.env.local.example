# Server-side (used by Next.js API routes/proxy — never exposed to browser)
BACKEND_URL=http://localhost:8765

# Client-side (exposed to browser — non-secret only)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8765
